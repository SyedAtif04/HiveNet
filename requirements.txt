# =============================================================
#  HiveNet — Consolidated Requirements
#  Install per service into its own virtual environment.
#
#  FinBot venv:    pip install -r requirements.txt --extra-index-url ... (section 1)
#  LogBot venv:    pip install -r requirements.txt (sections 2 + 3)
#
#  Or install all at once in a single env (development only):
#    pip install -r requirements.txt
# =============================================================


# -------------------------------------------------------------
# [1] FINBOT  (FastAPI financial service — port 8000)
# -------------------------------------------------------------

# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
python-multipart>=0.0.6

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
pgvector>=0.2.0
python-dotenv>=1.0.0

# AI / NLP
sentence-transformers>=2.2.0
spacy>=3.7.0
numpy>=1.24.0

# Forecasting
prophet>=1.1.0
pyarrow>=12.0.0          # required by Prophet

# OCR (bill scanning)
Pillow>=10.0.0
pytesseract>=0.3.10


# -------------------------------------------------------------
# [2] LOGBOT API  (FastAPI logistics service — port 8001)
# -------------------------------------------------------------

# Web framework (same as finbot, versions compatible)
# fastapi, uvicorn, pydantic, python-multipart — already listed above

# Data layer
# sqlalchemy, psycopg2-binary, python-dotenv — already listed above

# File handling
aiofiles>=23.2.0
python-dateutil>=2.8.2

# Inventory optimization
scipy>=1.10.0

# Stockout prediction (ML)
scikit-learn>=1.3.0
joblib>=1.3.0

# Semantic intent routing
# sentence-transformers, spacy — already listed above


# -------------------------------------------------------------
# [3] LOGBOT ML PIPELINE  (LightGBM demand forecasting — CLI)
# -------------------------------------------------------------

pandas>=2.0.0
lightgbm>=4.0.0
openpyxl>=3.1.0
tqdm>=4.65.0
requests>=2.31.0

# -------------------------------------------------------------
# Post-install steps (run once per venv after pip install):
#
#   python -m spacy download en_core_web_sm
#
# For Prophet on Windows, if installation fails:
#   pip install pystan==2.19.1.1 before installing prophet
# -------------------------------------------------------------
